<div><header class="mb-7"><p class="text-sm font-medium text-emerald-700">Organização</p><h1 class="mt-1 text-3xl font-semibold tracking-tight">Categorias</h1><p class="mt-1 text-sm text-slate-500">Dê contexto para cada entrada e saída.</p></header><section class="grid gap-5 lg:grid-cols-[340px_1fr]"><form wire:submit="save" class="card h-fit space-y-4"><h2 class="font-semibold">Nova categoria</h2><label class="field">Nome<input wire:model="name" placeholder="Ex.: Educação"></label><label class="field">Tipo<select wire:model="type"><option value="expense">Saída</option><option value="income">Entrada</option></select></label><button class="btn-primary w-full">Adicionar</button></form><section class="card"><div class="grid gap-6 md:grid-cols-2">@foreach(['income'=>'Entradas','expense'=>'Saídas'] as $key=>$label)<div><h2 class="mb-3 font-semibold">{{ $label }}</h2><div class="space-y-2">@forelse($categories->where('type',$key) as $category)<div class="flex items-center justify-between rounded-xl bg-stone-50 px-3 py-3"><span class="text-sm">{{ $category->name }}</span>@if(!$category->is_archived)<button wire:click="archive({{ $category->id }})" wire:confirm="Arquivar esta categoria?" class="text-xs text-slate-400">Arquivar</button>@else<span class="text-xs text-slate-400">Arquivada</span>@endif</div>@empty<p class="text-sm text-slate-400">Nenhuma categoria.</p>@endforelse</div></div>@endforeach</div></section></section></div>
